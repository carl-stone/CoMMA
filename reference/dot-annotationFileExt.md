# Detect annotation file format from file extension

Detect annotation file format from file extension

## Usage

``` r
.annotationFileExt(file)
```

## Value

Character string, either `"gff"` or `"bed"`.
