# Standardize annotation GRanges mcols to always have feature_type and name

Standardize annotation GRanges mcols to always have feature_type and
name

## Usage

``` r
.standardizeAnnotationMcols(gr, ext)
```

## Value

A `GRanges` object with standardized `feature_type` and `name` metadata
columns.
