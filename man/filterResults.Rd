% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/results_methods.R
\name{filterResults}
\alias{filterResults}
\alias{filterResults,commaData-method}
\title{Filter differential methylation results by significance thresholds}
\usage{
filterResults(object, ...)

\S4method{filterResults}{commaData}(
  object,
  padj = 0.05,
  delta_beta = 0.1,
  mod_type = NULL,
  motif = NULL,
  ...
)
}
\arguments{
\item{object}{A \code{\link{commaData}} object on which
\code{\link{diffMethyl}} has been run.}

\item{...}{Ignored.}

\item{padj}{Numeric. Maximum adjusted p-value threshold (inclusive).
Default \code{0.05}.}

\item{delta_beta}{Numeric. Minimum absolute effect size threshold
(\eqn{|\Delta\beta|}) (inclusive). Default \code{0.1}. Set to \code{0} to
disable filtering on effect size.}

\item{mod_type}{Character string or \code{NULL}. Passed to
\code{\link{results}} for optional modification type filtering.}

\item{motif}{Character vector or \code{NULL}. Passed to
\code{\link{results}} for optional sequence context motif filtering.}
}
\value{
A \code{data.frame} (same format as \code{\link{results}}) containing
only sites where \code{dm_padj <= padj} \strong{and}
\code{abs(dm_delta_beta) >= delta_beta}. Sites with \code{NA} values in
either column are excluded.
}
\description{
A convenience wrapper around \code{\link{results}} that filters sites by
adjusted p-value and absolute effect size.
}
\examples{
data(comma_example_data)
dm <- diffMethyl(comma_example_data, formula = ~ condition, mod_type = "6mA")
sig <- filterResults(dm, padj = 0.05, delta_beta = 0.2)
nrow(sig)

}
\seealso{
\code{\link{diffMethyl}}, \code{\link{results}}
}
