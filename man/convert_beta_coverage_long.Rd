% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingest_site_table.R
\name{convert_beta_coverage_long}
\alias{convert_beta_coverage_long}
\title{Convert generic long beta+coverage methylation table to canonical site schema}
\usage{
convert_beta_coverage_long(
  long_df,
  position_is_zero_based = FALSE,
  default_mod_base = "6mA",
  default_motif = "GATC"
)
}
\arguments{
\item{long_df}{A generic long-format methylation table.}

\item{position_is_zero_based}{Logical; if \code{TRUE}, \code{pos} is incremented by 1
during conversion. Defaults to \code{FALSE}.}

\item{default_mod_base}{Default \code{mod_base} when absent. Defaults to \code{"6mA"}.}

\item{default_motif}{Default \code{motif} when absent. Defaults to \code{"GATC"}.}
}
\value{
A validated canonical site table with backfilled count columns.
}
\description{
Convert generic long-format methylation data with beta and coverage columns
into CoMMA's canonical site schema. This is useful when callers already
report methylation fractions instead of modified/unmodified counts.
}
\details{
Required source columns are \code{seqname}, \code{pos}, \code{strand}, \code{beta}, \code{coverage},
\code{sample_id}, and \code{group}. Optional columns are \code{mod_base} and \code{motif}.
}
\examples{
long_df <- data.frame(
  seqname = c("chr1", "chr1"),
  pos = c(100L, 200L),
  strand = c("+", "-"),
  beta = c(0.7, 0.1),
  coverage = c(10L, 20L),
  sample_id = c("S1", "S2"),
  group = c("WT", "mut"),
  mod_base = c("5mC", "5mC")
)
convert_beta_coverage_long(long_df)
}
