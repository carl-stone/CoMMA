% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/writeBED.R
\name{writeBED}
\alias{writeBED}
\title{Write methylation calls as a BED9 file}
\usage{
writeBED(
  site_table,
  output_path,
  position_col = "Position",
  methyl_col = "beta",
  strand_col = "Strand",
  chrom = "U00096.3",
  track_name = "methylation",
  description = "Methylation",
  include_track_line = TRUE
)
}
\arguments{
\item{site_table}{Data frame of methylation calls.}

\item{output_path}{File path to write.}

\item{position_col}{Column name containing 1-based site positions.}

\item{methyl_col}{Column name containing methylation beta values in `[0, 1]`.}

\item{strand_col}{Column name containing strand values.}

\item{chrom}{Chromosome/contig label to write in BED `chrom` column.}

\item{track_name}{Track name used in the optional header line.}

\item{description}{Track description used in the optional header line.}

\item{include_track_line}{If `TRUE`, write a UCSC-style track header before
BED rows.}
}
\value{
Invisibly returns the BED data.frame written to `output_path`.
}
\description{
Convert a methylation site table into a BED9 table with an optional UCSC/IGV
track header and write it to disk.

\subsection{Input schema}{
`site_table` must contain:
\itemize{
\item `position_col`: 1-based genomic position (`integer`/`numeric`)
\item `methyl_col`: methylation fraction in `[0, 1]` (`numeric`)
\item `strand_col`: strand values (`"+"`, `"-"`, or `"."`)
}
}

\subsection{Return schema}{
Returns (invisibly) a `data.frame` with BED9 columns:
`chrom`, `chromStart`, `chromEnd`, `name`, `score`, `strand`,
`thickStart`, `thickEnd`, `itemRGB`.

Input positions are validated as 1-based coordinates and then converted to
BED coordinates (0-based, half-open):
\itemize{
\item `chromStart = position - 1`
\item `chromEnd = position`
}
}
}
\examples{
df <- data.frame(Position = c(10L, 25L), beta = c(0.2, 0.9), Strand = c("+", "-"))
out <- tempfile(fileext = ".bed")
writeBED(df, out, position_col = "Position", methyl_col = "beta", strand_col = "Strand")
}
