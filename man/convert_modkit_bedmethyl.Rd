% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingest_site_table.R
\name{convert_modkit_bedmethyl}
\alias{convert_modkit_bedmethyl}
\title{Convert modkit summarized bedMethyl-like output to canonical site schema}
\usage{
convert_modkit_bedmethyl(modkit_df, sample_id, group)
}
\arguments{
\item{modkit_df}{A data frame from modkit summarized output.}

\item{sample_id}{Sample identifier to assign to every row.}

\item{group}{Group/condition label to assign to every row.}
}
\value{
A validated canonical site table with derived \code{beta}.
}
\description{
Convert modkit summarized output (bedMethyl-like table) into CoMMA's
canonical site schema and validate it with \code{\link[=validate_site_table]{validate_site_table()}}.
}
\details{
Column mapping defaults:
\itemize{
\item \code{chrom} -> \code{seqname}
\item \code{start} (0-based) -> \code{pos} (1-based, \code{start + 1})
\item \code{strand} -> \code{strand}
\item \code{modified_primary_base} (or \code{mod_base} if already present) -> \code{mod_base}
\item \code{n_mod} -> \code{n_mod}
\item \code{valid_coverage} (or \code{n_total}) -> \code{n_total}
}
}
\examples{
modkit_df <- data.frame(
  chrom = c("chr1", "chr1"),
  start = c(99L, 199L),
  strand = c("+", "-"),
  modified_primary_base = c("6mA", "6mA"),
  n_mod = c(15, 8),
  valid_coverage = c(20, 10)
)
convert_modkit_bedmethyl(modkit_df, sample_id = "S1", group = "WT")
}
