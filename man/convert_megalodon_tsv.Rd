% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingest_site_table.R
\name{convert_megalodon_tsv}
\alias{convert_megalodon_tsv}
\title{Convert Megalodon-like per-site call table to canonical site schema}
\usage{
convert_megalodon_tsv(
  megalodon_df,
  sample_id,
  group,
  position_is_zero_based = FALSE
)
}
\arguments{
\item{megalodon_df}{A data frame with Megalodon-like per-site rows.}

\item{sample_id}{Sample identifier to assign to every row.}

\item{group}{Group/condition label to assign to every row.}

\item{position_is_zero_based}{Logical; if \code{TRUE}, \code{pos} is incremented by 1
during conversion. Defaults to \code{FALSE}.}
}
\value{
A validated canonical site table with derived \code{beta}.
}
\description{
Convert a Megalodon-like TSV/table into CoMMA's canonical site schema,
then validate with \code{\link[=validate_site_table]{validate_site_table()}}. This converter is designed to be
pragmatic across minor column-name differences.
}
\details{
Default column matching (first existing name is used):
\itemize{
\item sequence: \code{chromosome} or \code{contig} or \code{seqname}
\item position: \code{position} or \code{pos}
\item strand: \code{strand}
\item modified base: \code{mod_base} or \code{modified_base}
\item modified count: \code{n_mod} or \code{mod_count}
\item total count: \code{n_total} or \code{coverage} or \code{read_depth}
\item motif (optional): \code{motif}
}
}
\examples{
megalodon_df <- data.frame(
  chromosome = c("chr1", "chr1"),
  position = c(99L, 199L),
  strand = c("+", "-"),
  modified_base = c("6mA", "6mA"),
  mod_count = c(8L, 6L),
  read_depth = c(10L, 12L)
)
convert_megalodon_tsv(megalodon_df, sample_id = "S1", group = "WT", position_is_zero_based = TRUE)
}
